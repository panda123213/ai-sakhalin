# 🧠 Ежедневный дневник инженера ИИ

**Правило:** Записываю каждый день, что узнал, с чем столкнулся и куда двигаюсь.

---

## 28.07.2026 (День 1) — Первое знакомство с Git и Ollama

**Что сделал:**
- Установил Git и привязал к GitHub.
- Запустил локальную модель `llama3.2:3b` через Ollama.
- Написал первый скрипт `ollama-test.py`, который отправил запрос к ИИ.

**Что понял:**
- Git — это не просто "сохранить", а история всех изменений. Коммиты — это моя страховка.
- ИИ (Ollama) работает локально и не требует интернета.

**Ошибки/Инсайты:**
- Долго не мог понять, как пушить на GitHub из-за авторизации. Решил через токен.
- `requests` сначала не работал — оказывается, его надо установить через `pip`.

**Полезное:**
- Запомнил: `Ctrl + S` → `git add .` → `git commit -m "..."` → `git push`.

---

## 29.07.2026 (День 2) — Память и управление поведением ИИ

**Что сделал:**
- Написал `chat_with_memory.py` — агент запоминает диалог через файл.
- Научился управлять `temperature` (сделал модель более строгой).
- Создал `git_cheatsheet.md` и обновил `README.md`.

**Что понял:**
- Чтобы ИИ слушался, нужно грамотно писать `system_role`. 
- `temperature = 0.3` — идеально для чётких ответов, `0.7+` — для креатива.
- Файлы в Git бывают `U`, `A` и `M` — это их жизненный цикл.

**Ошибки/Инсайты:**
- Обновил код, а на GitHub он не появился — потому что забыл сделать `git push`.
- Файл `git_cheatsheet.md` висел пустым (`0 КБ`), хотя я нажимал `Ctrl + S`. Закрыл-открыл VS Code — проблема решилась.


## 30.07.2026 (День 3) — Агент для чтения файлов

**Что сделал:**
- Написал `file_reader_agent.py`.
- Настроил `system_role` для контроля поведения.
- Научился передавать содержимое файла в ИИ.

**Что понял:**
- ИИ — буквален, и промпт нужно формулировать чётко.
- `system_role` управляет поведением модели.
- Чтение файлов открывает путь к созданию агентов-аналитиков.
